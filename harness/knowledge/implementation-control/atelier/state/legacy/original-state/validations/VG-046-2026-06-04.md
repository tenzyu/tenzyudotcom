# VG-046 Proof Artifact

```yaml
record_id: VG-046-2026-06-04
gate_id: VG-046
purpose: Parallel-packet conflict detection
fixture_id: parallel_conflict_check_v1
ran_at: 2026-06-04T03:00:00Z
recorded_by: control-doc-repair
status: passed
command: bun nx run atelier:test -- --testPathPattern=parallel-conflict-checker
command_resolved_from: state/gates/structured-gates-2026-06-04.yaml#VG-046
resolved_from_algorithm: |
  Resolve and run the parallel-conflict-checker test target via
  Nx test target. The test target runs
  product/apps/atelier/src/__tests__/parallel-conflict-checker.test.ts
  which exercises the core module at
  product/apps/atelier/src/core/parallel-conflict-checker.ts.
affected_packets:
  - at-ctrl-009
affected_invariants:
  - AT-INV-080
proof_summary: |
  The parallel-conflict-checker test target ran 19 tests covering:
  empty in-flight list (passed), disjoint allowed_files (passed),
  exact allowed_files intersection (failed with one report),
  candidate file under an in-flight directory glob (failed),
  two directory globs sharing a parent (failed),
  candidate forbidden_roots intersecting an in-flight allowed file
  (failed), in-flight forbidden_roots intersecting a candidate
  allowed file (failed), fixture family overlap (failed), command
  surface overlap (failed), generated_state_paths overlap (failed),
  durable_evidence_paths overlap (failed), multiple conflict kinds
  from a single in-flight packet (failed with all three kinds
  reported), conflict aggregation across multiple in-flight packets
  (failed with two packet ids), deterministic sorted
  conflicting_packet_ids, file load and parse, missing
  in_flight_packets key, full file-load + check integration, full
  disjoint case, and ran_at / candidate_packet_id fields. All 19
  tests passed. The full atelier test suite ran 178 tests with 0
  failures.
```

## Command Output

```text
$ bun nx run atelier:test -- --testPathPattern=parallel-conflict-checker

> nx run atelier:test --testPathPattern=parallel-conflict-checker

> bun run test --testPathPattern=parallel-conflict-checker

bun test v1.3.13 (bf2e2cec)
$ bun test "--testPathPattern=parallel-conflict-checker"

src/__tests__/parallel-conflict-checker.test.ts:
  ✓ parallel-conflict-checker (VG-046) > passes when in-flight list is empty
  ✓ parallel-conflict-checker (VG-046) > passes when candidate allowed_files are disjoint from in-flight allowed_files
  ✓ parallel-conflict-checker (VG-046) > fails on exact allowed_files intersection
  ✓ parallel-conflict-checker (VG-046) > fails when a candidate file is under an in-flight directory glob
  ✓ parallel-conflict-checker (VG-046) > fails when two directory globs share a parent
  ✓ parallel-conflict-checker (VG-046) > fails when candidate forbidden_roots include an in-flight allowed file
  ✓ parallel-conflict-checker (VG-046) > fails when an in-flight forbidden_roots include a candidate allowed file
  ✓ parallel-conflict-checker (VG-046) > fails on fixture family conflict
  ✓ parallel-conflict-checker (VG-046) > fails on command surface conflict
  ✓ parallel-conflict-checker (VG-046) > fails on generated_state_paths conflict
  ✓ parallel-conflict-checker (VG-046) > fails on durable_evidence_paths conflict
  ✓ parallel-conflict-checker (VG-046) > reports multiple conflict kinds from a single in-flight packet
  ✓ parallel-conflict-checker (VG-046) > aggregates conflicts across multiple in-flight packets
  ✓ parallel-conflict-checker (VG-046) > returns a sorted, deterministic conflicting_packet_ids list
  ✓ parallel-conflict-checker (VG-046) > loadInFlightPackets reads and parses the in-flight file
  ✓ parallel-conflict-checker (VG-046) > loadInFlightPackets returns empty list for missing in_flight_packets key
  ✓ parallel-conflict-checker (VG-046) > runConflictCheck integrates file load and check
  ✓ parallel-conflict-checker (VG-046) > candidate with disjoint allowed_files and disjoint command surfaces passes
  ✓ parallel-conflict-checker (VG-046) > result includes ran_at timestamp and candidate packet id
 178 pass
   0 fail
 600 expect() calls
Ran 178 tests across 13 files. [1.57s]

NX   Successfully ran target test for project atelier
```

## In-flight File Snapshot

```yaml
file_id: in-flight-2026-06-04
recorded_at: 2026-06-04T03:00:00Z
schema: atelier.in-flight-packets/v1
in_flight_packet_count: 0
in_flight_packets: []
```

The in-flight list is empty at the time of this proof. This is the
clean baseline the VG-046 check uses to compute the
`conflicting_packet_ids` set on every parallel-dispatch attempt.

## Forbidden-Action Audit

| Forbidden action | Found in at-ctrl-009 diff? |
|---|---|
| weakening gates | No (VG-046 is a new gate, additive) |
| deleting dependencies | No |
| broadening completion criteria | No (VG-046 is a parallel-dispatch safety check) |
| relaxing product-spec immutability | No (no product spec was edited) |
| adding compatibility aliases for removed commands | No |
| broadening fixture scope without matrix-backed reason | No (the parallel_conflict_check_v1 fixture is the check itself, backed by an implemented module) |
| narrowing expected diff shape to hide required work | No |
| downgrading blocker severity without evidence | No (VG-046 is P1) |
| converting executable requirements into assumptions | No (the new gate is executable_now: true) |
| allowing pending commands to satisfy phase gates | No (the check is a real, fail-closed implementation) |

All ten forbidden actions: clear.
