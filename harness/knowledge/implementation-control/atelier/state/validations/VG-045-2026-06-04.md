# VG-045 Proof Artifact

```yaml
record_id: VG-045-2026-06-04
gate_id: VG-045
purpose: Fixture alias consistency
fixture_id: fixture_alias_registry_v1
ran_at: 2026-06-04T01:00:00Z
recorded_by: control-doc-repair
status: passed
command: bun nx run atelier:test -- --testPathPattern=fixture-alias-consistency
command_resolved_from: state/gates/structured-gates-2026-06-04.yaml#VG-045
resolved_from_algorithm: resolve and run fixture-alias-consistency.test.ts via Nx test target
affected_packets:
  - at-ctrl-006
affected_invariants:
  - AT-INV-069
proof_summary: |
  The alias consistency test target ran 12 tests covering unique
  fixture_ids, non-empty required fields, closed status vocabulary,
  command_file path existence, input_path and expected_path existence
  for executable rows, gate_id references in the gate table and
  structured gate record, closed gate_id pattern, non-null
  negative_case_id references in the matrix, and code-safe fixture
  token naming. All 12 tests passed. The full atelier test suite
  ran 122 tests with 0 failures.
```

## Command Output

```text
$ bun nx run atelier:test -- --testPathPattern=fixture-alias-consistency

> nx run atelier:test --testPathPattern=fixture-alias-consistency

> bun run test --testPathPattern=fixture-alias-consistency

bun test v1.3.13 (bf2e2cec)
$ bun test "--testPathPattern=fixture-alias-consistency"

src/__tests__/fixture-alias-consistency.test.ts:
  ✓ fixture alias consistency (VG-045) > registry is parseable and has the expected schema fields
  ✓ fixture alias consistency (VG-045) > every fixture_id is unique
  ✓ fixture alias consistency (VG-045) > every row has the required fields with non-empty values
  ✓ fixture alias consistency (VG-045) > every status is in the closed vocabulary
  ✓ fixture alias consistency (VG-045) > every command_file path exists on disk OR the row is pending/oracle_gap
  ✓ fixture alias consistency (VG-045) > every input_path and expected_path exists on disk OR the row is pending/oracle_gap
  ✓ fixture alias consistency (VG-045) > every gate_id references a real VG-NNN in the gate table or structured record
  ✓ fixture alias consistency (VG-045) > every gate_id matches the closed pattern VG-<digits>[<letter>]
  ✓ fixture alias consistency (VG-045) > every non-null negative_case_id references a real negative case in the matrix
  ✓ fixture alias consistency (VG-045) > no pending_command_implementation row is used as the sole phase-gate evidence
  ✓ fixture alias consistency (VG-045) > every executable row has its command_file, input_path, and expected_path on disk
  ✓ fixture alias consistency (VG-045) > every fixture_id appears as a code-safe token (no spaces in path-derived ids)
 122 pass
 0 fail
 491 expect() calls
Ran 122 tests across 11 files. [1027.00ms]

NX   Successfully ran target test for project atelier
```

## Alias Registry Snapshot

```yaml
registry_id: fixture-alias-registry-2026-06-04
recorded_at: 2026-06-04T01:00:00Z
schema: atelier.fixture-alias-registry/v1
vocabulary:
  status: [executable, pending_command_implementation, oracle_gap]
  closed_gate_id_pattern: "^VG-[0-9]+[A-Z]?$"
fixture_count: 45
executable_count: 5
pending_command_implementation_count: 40
oracle_gap_count: 0
```

The five executable rows are:

- `product_spec_hash_baseline` (VG-001)
- `repo_inventory_2026_06_04` (VG-000)
- `command_discovery_2026_06_04` (VG-000)
- `all-assigned-fixtures` (VG-004)
- `fixture_alias_registry_v1` (VG-045)

The 40 `pending_command_implementation` rows correspond to contract
fixtures from `CONTRACT_TEST_MATRIX.md` §1a, §2, §2a, and §2b, plus
gate fixtures from the structured gate records (e.g.
`write_authority_minimum_v1`, `e2e_mvp_wedge_v1`,
`stale_command_grep_v1`, etc.). Every `pending_command_implementation`
row has a placeholder `command.ts` file at
`product/apps/atelier/src/__tests__/fixtures/<id>/command.ts` that
throws `fixture_not_yet_implemented: <id>`. No placeholder is a
compatibility alias; the throw is a real, fail-closed implementation.

## Forbidden-Action Audit

| Forbidden action | Found in at-ctrl-006 diff? |
|---|---|
| weakening gates | No |
| deleting dependencies | No |
| broadening completion criteria | No |
| relaxing product-spec immutability | No |
| adding compatibility aliases for removed commands | No |
| broadening fixture scope without matrix-backed reason | No |
| narrowing expected diff shape to hide required work | No |
| downgrading blocker severity without evidence | No (BLK-FIXTURE-ALIAS-001 closed with proof; severity unchanged) |
| converting executable requirements into assumptions | No |
| allowing pending commands to satisfy phase gates | No (pending rows explicitly fail closed; orchestrator rule preserved) |

All ten forbidden actions: clear.
