{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "atelier.risk/v1",
  "type": "object",
  "required": ["schema", "risk_id", "source_section_id", "text", "severity", "provenance_kind", "provenance_ref", "extraction_status"],
  "properties": {
    "schema": { "const": "atelier.risk/v1" },
    "risk_id": { "type": "string" },
    "source_section_id": { "type": "string" },
    "text": { "type": "string" },
    "severity": { "enum": ["P0", "P1", "P2"] },
    "provenance_kind": { "enum": ["deterministic_fact", "llm_extracted", "legacy_promoted", "manual_control_record"] },
    "provenance_ref": { "type": "string" },
    "extraction_status": { "enum": ["pending", "running", "accepted", "rejected", "skipped"] }
  }
}
