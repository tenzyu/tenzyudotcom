# Handoff: RUN-run-update-atelier-product-spec-in-readme-md-81c786517f

## Run Summary

Updated the Atelier Product Spec and Roadmap to make the provided control-plane direction canonical.

## Assigned Roles

- `role.core.implementer`

## Required Knowledge Loaded

- Run context generated by Atelier.
- Root AGENTS adapter.
- Current Atelier product spec and roadmap.
- Current Atelier app README, package targets, public exports, and core module structure.

## What Changed

- `harness/knowledge/product-specs/atelier/README.md`
  - Repositioned Atelier as an Agentic Software Development Control Plane.
  - Defined system state as `Git working tree + Atelier Artifact Graph + Event Log`.
  - Added core kernel, planes, artifact graph, event log, ownership modes, reconciler semantics, controls, governance, agent loop, swarm, and target GUI model.
  - Reframed Markdown/Knowledge Cards/frontmatter as v1 Knowledge Plane implementation rather than final product center.

- `harness/knowledge/product-specs/atelier/ROADMAP.md`
  - Kept M0-M12 as shipped v1 baseline.
  - Added M13-M20 control-plane roadmap: Artifact Graph Kernel, Event Log/Reconciler, Selector v2, Control Mechanism Registry, Artifact Graph UI, Governance Runtime, Agent Loop, and Swarm Coordination.
  - Replaced the old first implementation order with v2 implementation slices.

## Why It Changed

The owner clarified that the prior "Markdown source of truth / Atelier compiler" model is too narrow. Atelier should treat repo knowledge, controls, permissions, tasks, agents, traces, and generated files as artifacts in a reconciled control system.

## Affected Files

- `harness/knowledge/product-specs/atelier/README.md`
- `harness/knowledge/product-specs/atelier/ROADMAP.md`
- ignored `.harness/generated/*` cache files refreshed by `atelier index`
- this run's `worklog.md`, `verification.md`, and `handoff.md`

## Validation Result

- Targeted Atelier checks passed.
- Generated index freshness passed after refresh.
- Dependency policy passed.
- Broad affected check failed only because of an unrelated existing `web:check` TypeScript config issue.

## Remaining Risks

- The new v2 roadmap is documentation only; M13-M20 are not implemented yet.
- `web:check` still fails outside this change because `product/apps/web/tsconfig.json` needs the TypeScript 7 deprecation handling for `baseUrl`.

## Follow-Up Tasks

- Implement M13: Artifact Graph Kernel.
- Fix the unrelated `web:check` TypeScript config failure in a separate run.

## Knowledge Updates Made Or Proposed

- Durable knowledge updated directly in the Atelier product spec and roadmap.
- No separate knowledge proposal was created because the user explicitly requested the product spec update.
