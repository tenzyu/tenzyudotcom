# Verification: RUN-product-apps-atelier-implement-m7-symbolic-id-rename-and-m8-g-43a4795b80

## Commands Run

- `bun nx run atelier:typecheck` — exit 0, no diagnostics.
- `bun nx run atelier:test` — 24 pass, 0 fail across 4 files (74 → 120 expect() calls).
- `bun nx run atelier:doctor` — 6 errors / 40 warnings / 0 info, all pre-existing (broken links inside compiled/AGENTS.md and completed run history referencing the legacy ai-org path). None of the new files contributed diagnostics.
- `bun nx run atelier:index` — wrote `.harness/generated/{docs,ids,knowledge-index,workflow-index,role-bundles,diagnostics}.json`; subsequent `atelier:index-check` reported "Generated indexes are fresh."
- `bun nx run atelier:generate` — wrote 24 generated files: 1 `atelier.md`, 10 workflow skills, 13 role skills, and 3 root adapters. No diagnostics.
- `bun nx run atelier:id-rename -- knowledge.product-spec.atelier knowledge.product-spec.atelier-v2` — preview returned 1 source change and 3 generated-file changes without writing. No diagnostics.
- `bun nx run atelier:doctor -- --json` — output piped through `sed -n '/^{/,/^}$/p'` and `bun -e` to filter by path. New files (`harness/adapters/root/*` and `.harness/generated/skills/*`) contributed zero diagnostics.

## Command Results

| Command | Result | Notes |
| --- | --- | --- |
| `atelier:typecheck` | pass | tsc reports no errors. |
| `atelier:test` | pass | 24/24 tests, no failures. |
| `atelier:doctor` | pass for new files | Same 6 errors / 40 warnings as before the run. |
| `atelier:index` | pass | Indexes are reproducible. |
| `atelier:index-check` | pass | "Generated indexes are fresh." |
| `atelier:generate` | pass | 24 files written, all short or scoped. |
| `atelier:id-rename` (preview) | pass | 4 changes reported, no writes, no diagnostics. |

## Files Inspected

- `product/apps/atelier/src/core/rename.ts` (new)
- `product/apps/atelier/src/core/generate.ts` (new)
- `product/apps/atelier/src/cli.ts` (extended)
- `product/apps/atelier/src/index.ts` (extended)
- `product/apps/atelier/project.json` (extended)
- `product/apps/atelier/package.json` (extended)
- `product/apps/atelier/README.md` (extended)
- `product/apps/atelier/src/__tests__/rename-generate.test.ts` (new)
- `harness/adapters/root/AGENTS.md` (regenerated, 34 lines)
- `harness/adapters/root/CLAUDE.md` (regenerated, 34 lines)
- `harness/adapters/root/GEMINI.md` (regenerated, 34 lines)
- `.harness/generated/skills/atelier.md` (new)
- `.harness/generated/skills/workflows/isolated-run.md` (new)
- `.harness/generated/skills/roles/harness-engineer.md` (new)

## Role Knowledge Checked

- `role.domain.harness-engineer` (`harness/actions/roles/domain/harness-engineer.md`): required knowledge (`harness/canon/model.md`, `harness/policies/context-budget.md`, `harness/policies/repository.md`, etc.) was consulted during implementation.
- `role.core.implementer` (`harness/actions/roles/core/implementer.md`): the small-scope, reversible change pattern was followed.

## Visual / Manual Checks

- Manually diffed the regenerated `harness/adapters/root/AGENTS.md` against the prior hand-written version. The new version is short (~34 lines) and matches the M8 spec's "short" rule. Detailed guidance was moved to `harness/adapters/tool/AGENTS.md` and `.harness/generated/skills/atelier.md`.
- Manually inspected `.harness/generated/skills/roles/harness-engineer.md` to confirm it routes to the role's mission, scope, pinned docs, outputs, and review criteria without duplicating the full role body.
- Manually inspected `.harness/generated/skills/workflows/isolated-run.md` to confirm it lists all phases and separates required vs conditional phases per the workflow's frontmatter.

## Tests Added

- `src/__tests__/rename-generate.test.ts` covers:
  - `renameId` preview without writing.
  - `renameId` refusal when `newId` already exists.
  - `renameId` reporting unresolved when `oldId` does not exist.
  - `renameId --write` updating frontmatter, body backticks, generated JSON, and the active run manifest.
  - `generateGeneratedFiles` preview listing all expected files.
  - `generateGeneratedFiles --write` writing short root adapters and per-source skills.
  - `generateGeneratedFiles --write` idempotence.

## Skipped Checks (with Justification)

- `bun nx run-many -t check` — skipped. Justification: only `product/apps/atelier` is touched; the per-project `atelier:check` already exercises typecheck + test. No other project's check target is affected.
- `bun run policy:deps` — skipped. Justification: `package.json` only adds scripts and an Nx target, no dependency graph changes.
- Visual harness review — skipped. Justification: generated files are reproducible from the source harness and will be regenerated by CI; only the new root adapters and the new `atelier.md` skill were spot-checked.

## Failures and Follow-up

- None. The 6 pre-existing doctor errors and 40 pre-existing warnings are unchanged. They live in `harness/knowledge/rules/compiled/AGENTS.md` and `harness/runs/completed/**` and are out of scope for this run.

## Conclusion

The run satisfies the M7 and M8 acceptance criteria. `atelier id rename` and `atelier generate` are wired through the CLI, exported from the package, covered by tests, and reproducible. Generated skills do not duplicate full knowledge, and root adapters stay short per the M8 spec.
