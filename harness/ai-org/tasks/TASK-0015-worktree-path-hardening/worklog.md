# Worklog

- Reviewed the canonical AI-org charter, repo map, harness overview, task lifecycle, handoff, verification, and worktree workflow.
- Updated the worktree isolation workflow to require `projectRoot/.worktrees/<task-slug>` and removed the parent-sibling fallback.
- Reinforced the same path rule in task intake, implementation, git guardrails, root `.gitignore`, and root `AGENTS.md`.
- Noted that the repository already has unrelated dirty files in `bun.lock`, `product/apps/web/package.json`, and `tsconfig.base.json`.
