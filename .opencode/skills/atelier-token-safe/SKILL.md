# Skill: atelier-token-safe

Use this whenever working on Atelier Autopoiesis.

1. Never Read `.atelier/v0/**` broadly.
2. Use `find`/`rg`/`wc -c` before reading.
3. Cap subagent output to findings and paths, not content.
4. Materialize large analysis into repo artifacts, then return artifact paths only.
5. Stop the run if token telemetry shows breach; do not continue inside the same transcript.
