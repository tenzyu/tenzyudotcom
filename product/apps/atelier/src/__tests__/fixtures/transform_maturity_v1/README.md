# Fixture: transform_maturity_v1

Placeholder v5.1 layout scaffolding for fixture `transform_maturity_v1`.

## Layout

This directory follows the v5.1 fixture layout from
`CONTRACT_TEST_MATRIX.md` §1a:

- `input.ts` — placeholder for fixture input data
- `expected.ts` — placeholder for fixture expected output
- `README.md` — this file
- `command.ts` — placeholder throw that fails closed

## Status

The alias registry records `status: pending_command_implementation`
for this row. `command.ts` throws
`fixture_not_yet_implemented: transform_maturity_v1`. A future fixture-author
packet will replace the placeholder contents with the real input,
expected, README, and command.

## Provenance

See the registry row for `transform_maturity_v1` in
`harness/knowledge/implementation-control/atelier/state/traceability/fixture-alias-registry-2026-06-04.yaml`
for the assigned DAG node, gate, and source spec section.
