import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Foundations/Radius" />

# Radius

Radius tokens are derived from `--radius` in `src/index.css`. Component stories
should verify that compact controls, panels, and overlays keep consistent corner
treatment across light and dark surfaces.

