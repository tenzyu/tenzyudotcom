import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Foundations/Typography" />

# Typography

The package uses Noto Sans Variable as the base font through `src/index.css`.
Storybook typography examples should use product-facing text density instead of
marketing-scale type unless the component itself requires it.

