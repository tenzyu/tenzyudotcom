# AGENTS.md

Canonical agent instructions live in `harness/README.md` and `harness/adapters/root/AGENTS.md`.

Start at `harness/canon/model.md`, then load only the relevant `knowledge/`, `policies/`, `actions/`, and `runs/` context for the task.
